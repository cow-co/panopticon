language: node_js

node_js:
  - 14

matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required

notifications:
  email: false

deploy:
  provider: script
  script: npm run electron-pack
  skip_cleanup: true
  on:
    branch: main

branches:
  only:
  - main